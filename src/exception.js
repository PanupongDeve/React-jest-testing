const getRectArea = (width, height) => {
    if (isNaN(width) || isNaN(height)) {
        throw "Parameter is not a number!";
      }
}

// try {
//     compileAndroidCode();
//   }
//   catch(e) {
//     console.error(e);
//   }

export default getRectArea;

